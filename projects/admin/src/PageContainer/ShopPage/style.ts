import styled from '@emotion/styled';

export const ShopText = styled.h2`
  ${({ theme }) => theme.typo.h2}
  color: ${({ theme }) => theme.color.black};
  font-weight: 600;
  width: 1312px;
  margin: 60px 0 28px;
`;

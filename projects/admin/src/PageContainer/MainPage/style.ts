import styled from '@emotion/styled';

export const Modal = styled.dialog`
  width: 350px;
  height: 385px;
  border-radius: 20px;
  background: #fbfbfb;
`;

export const Wrapper = styled.div`
  position: absolute;
  right: 20vw;
  top: 75vh;
  z-index: 3;
`;

'use client';

import ScoringCarousel from '.';
import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'admin/ScoringCarousel',
  component: ScoringCarousel,
} as Meta<typeof ScoringCarousel>;

type Story = StoryObj<typeof ScoringCarousel>;

export const Admin: Story = {};

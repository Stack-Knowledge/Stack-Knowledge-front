'use client';

import ShopPage from '.';

import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'client/Page/ShopPage',
  component: ShopPage,
} as Meta<typeof ShopPage>;

type Story = StoryObj<typeof ShopPage>;

export const Primary: Story = {};
